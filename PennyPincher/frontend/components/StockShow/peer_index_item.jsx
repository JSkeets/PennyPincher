import React from "react";
import { Link } from "react-router-dom";

const ReviewIndexItem = ({ peer }) => {
	console.log(peer);
	return <li className="news-index-item" />;
};

export default ReviewIndexItem;
